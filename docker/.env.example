# =============================================================================
# Nebula Commander Docker Configuration
# =============================================================================
# Copy this file to .env and customize the values
# cp .env.example .env

# =============================================================================
# Port Configuration
# =============================================================================

# Port for the frontend (Nginx)
FRONTEND_PORT=80

# Port for the backend (FastAPI) - typically not exposed directly
# BACKEND_PORT=8081

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite database URL: use four slashes for absolute path (so DB is at /var/lib/..., not CWD/var/lib/...)
# NEBULA_COMMANDER_DATABASE_URL=sqlite+aiosqlite:////var/lib/nebula-commander/db.sqlite

# =============================================================================
# Certificate Storage
# =============================================================================

# Path to store Nebula CA and certificates (inside container)
# CERT_STORE_PATH=/var/lib/nebula-commander/certs

# =============================================================================
# JWT Authentication
# =============================================================================

# JWT secret key for signing tokens
# IMPORTANT: Change this in production!
JWT_SECRET_KEY=change-this-to-a-secure-random-string

# Alternatively, mount a secret file (more secure)
# JWT_SECRET_FILE=/path/to/jwt-secret.txt
# JWT_SECRET_FILE_CONTAINER=/run/secrets/jwt-secret

# JWT algorithm (HS256, HS384, HS512)
JWT_ALGORITHM=HS256

# JWT token expiration in hours
JWT_EXPIRATION_HOURS=24

# =============================================================================
# OIDC Configuration (Optional)
# =============================================================================
# Uncomment and configure to use external OIDC provider (Authelia, Authentik, etc.)

# OIDC issuer URL (e.g., https://auth.example.com)
# OIDC_ISSUER_URL=

# OIDC client ID
# OIDC_CLIENT_ID=nebula-commander

# =============================================================================
# CORS Configuration
# =============================================================================

# Allowed CORS origins (comma-separated or * for all)
CORS_ORIGINS=*

# =============================================================================
# Debug Mode
# =============================================================================

# Enable debug mode (enables /dev-token endpoint)
# WARNING: Never enable in production!
DEBUG=false
