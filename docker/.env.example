# =============================================================================
# Nebula Commander Docker Infrastructure Configuration
# =============================================================================
# Copy this file to .env and customize the values
# cp .env.example .env
#
# Backend-specific settings (database, JWT, OIDC, CORS, debug) are configured
# in env.d/backend - see env.d/backend.example

# =============================================================================
# Port Configuration
# =============================================================================

# Port for the frontend (Nginx)
FRONTEND_PORT=80

# Port for the backend (FastAPI) - typically not exposed directly
# BACKEND_PORT=8081

# =============================================================================
# Keycloak Configuration (when using docker-compose-keycloak.yml)
# =============================================================================

# Port for Keycloak
# KEYCLOAK_PORT=8080

# =============================================================================
# JWT Secret File (Optional - for mounting secrets)
# =============================================================================

# Path to JWT secret file on host (will be mounted to container)
# More secure than setting JWT_SECRET_KEY in env.d/backend
# JWT_SECRET_FILE=/path/to/jwt-secret.txt
# JWT_SECRET_FILE_CONTAINER=/run/secrets/jwt-secret
