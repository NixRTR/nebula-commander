# Example environment for ncclient (systemd: /etc/default/ncclient, launchd: set in plist, Windows: set in Task or script)
# Copy to /etc/default/ncclient on Linux and set your server URL.

NEBULA_COMMANDER_SERVER=https://your-nebula-commander.example.com

# Optional (defaults shown):
# NEBULA_COMMANDER_OUTPUT_DIR=/etc/nebula
# NEBULA_COMMANDER_INTERVAL=60
# NEBULA_COMMANDER_TOKEN_FILE=/etc/nebula-commander/token
# NEBULA_COMMANDER_NEBULA=/usr/bin/nebula
# NEBULA_COMMANDER_RESTART_SERVICE=nebula
